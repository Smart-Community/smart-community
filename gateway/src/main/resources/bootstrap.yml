server:
  port: 80
---
spring:
  profiles: dev
  application:
    name: gateway
  cloud:
    config:
      label: dev
      name: eureka,redis,${spring.application.name}
      uri: http://localhost:8099/
