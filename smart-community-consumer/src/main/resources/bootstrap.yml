server:
  port: 0
feign:
  hystrix:
    enabled: true
---
spring:
  profiles: dev
  application:
    name: consumer
  cloud:
    config:
      label: dev
      name: eureka,redis,${spring.application.name}
      uri: http://localhost:8099/